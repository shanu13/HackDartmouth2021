<%- include("partials/header") -%>

<% for(let post of posts){ %>

<script src="https://code.jquery.com/jquery-3.5.1.js" integrity="sha256-QWo7LDvxbWT2tbbQ97B53yJnYU3WhH/C8ycbRAkjPDc=" crossorigin="anonymous"></script>

<script type="text/javascript">
  var c = 0;//we will insert the value of c(count) in our database and call it here
</script>

<div>
    <div class="container-fluid card">
        <h1><%= post.title %></h1>
          <p><%= post.name %>
        <div class="row">
          <div class="col">
            <img src="<%= `${post.imageUrl}`  %>" class=" img-thumbnail " alt="Blog Image" >  
          </div>
          <div class="col">
            <p><%= post.content%></p>
            <p> <%= post.techStack%>  </p>
            <p><%= post.timezone %></p>
            <p><%= post.education.university %> , <%= post.education.year %>  </p>
            <a href="mailto:<%= post.email %>"> Send Mail</a>
           <a href="https://<%= post.link.github %>" target="_blank">Github</a>
           <a href="https://<%= post.link.linkedin %>" target="_blank">Linkedin</a>
           <a id="message" href="/message/<%=post.user.toString() %>" data-user = "<%= post.user%>"> Message
          </a>
            
            <!-- <button class="visit">View Blog</button> -->
          </div>
        </div>
        <div class="row">
          <div class="col">
           
            <!-- <form action="/delCnf/<%= post._id %>" method="post">
              <div class="form-group">
                <input type="checkbox" onChange="this.form.submit()" name="checkbox" >
              </div>
            </form> -->
              <% if(post.user.toString() == id) {  %> 
            <button id="deletePost" class="btn btn-primary" data-postid ="<%= `${post._id }`%>"> Delete Post </button>
            <% } %> 
            
            
            <!-- <div class="like_count">
              Total number of likes--
              <span> 0 </span>
              <script>
              $(document).ready(function(){
                $(".like").click(function(){
                    c=c+1;
                     console.log(c);
                     $(".like_count span").html(c)
                     
                      });
                         });
                //document.write(c) -->
                
               </script>
                  
            </div>
            
          </div>
        </div>
      </div>
</div>

<script>
  function like(){
   fetch('/like',{ 
       method : 'POST',
       headers: {
        "Content-type": "application/json; charset=UTF-8"
    }
   }).then(response => response.json)
     .then(data => {
         console.log(data)
     })
} 
</script>

<% } %>





<%- include("partials/footer") -%>
